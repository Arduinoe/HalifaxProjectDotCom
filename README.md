# Halifax

Static website for Halifax.

## Structure

- `index.html` contains the page structure and content.
- `styles.css` contains layout and responsive styling.
- `script.js` contains menu, links pop-up, and gallery lightbox interactions.
- `assets/` contains local images and fonts.
